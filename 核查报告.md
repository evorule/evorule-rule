# evorule-rule 仓检查报告（对照 30-39 + 41-47）

> 8 个 commit，2026-08-21 ~ 08-22，~340 KB 源码，working tree 干净。**MVP 数据模型 + 鉴权 + API 面基本按设计落地**；但**与设计基线有几处明确走偏**，按"重要性 × 是否在 MVP 范围内"分级标注。

---

## 1. 完成度矩阵

| 决策点 / 主题 | 文档号 | 实现位置 | 状态 |
|---|---|---|---|
| 独立仓独立部署 | 30 §A.1 | Cargo.toml 无 evorule 仓依赖 | ✅ 100% |
| LLM 复用 evo-agent | 30 §A.3 / 37 | `src/llm_client/`（20.9KB） | ✅ 100% |
| **数据模型** | 31 决策点① | `src/model/` 9 子模块 | ✅ 100% |
| **版本单位 = 数据集** | 32 决策点② | `model/versioning.rs` + `bump/validate` | ✅ 100% |
| **版本语义** | 33 决策点③ | `Version` + `BumpKind` + `VersionSelection` + `resolve/` 解析算法 | ✅ 100% |
| **5 态生命周期** | 34 决策点④ | `Lifecycle` + `StateChange` 审计 + `state_history` only-append | ✅ 100% |
| **数据源绑定三层** | 35 决策点⑤ | `DataDependencies` + `SourceBinding` + `ServiceTemplate` + 模板 bind() | ✅ 100% |
| **快照包** | 36 决策点⑥ | `src/bundle/` 26.4KB：导出 / 导入 / 裁剪 / 预检 / 状态 | ✅ 100% |
| **LLM 服务接口** | 37 决策点⑦ | `LlmClient` + 3 个命名 op + llm_audit 落库 | ✅ 100% |
| **多租户 + 权限** | 38 决策点⑧ | 单租户实例 + 四角色 + 7 Action | ⚠️ **60%**（两层租户只做组织层） |
| **白标 brand.yaml** | 39 决策点⑨ | — | ❌ **0%** |
| **正交 A 认证** | 41 §5 / 43 | `src/auth/` 21.9KB：注册/登录/刷新 + JWT + AuthContext | ⚠️ **90%**（密码哈希走偏） |
| **正交 B API 面** | 41 §6 / 44 | `src/api/` 8 个 handlers + axum Router 50+ 端点 | ✅ 90% |
| **批次 1 PostgreSQL** | 41 §2 | `rusqlite` (bundled) | ❌ **0%**（按 MVP 阶段可理解） |
| 批次 2 分布式订阅 | 41 §3 | — | ❌ 0%（按"按需"原则） |
| 批次 2 回写闭环 | 41 §3 / 36 §6 | 只定 schema | ✅ 符合 MVP |
| 批次 2 异步 LLM 队列 | 41 §3 / 37 §4 | 同步实现 + `task_id` 预留 | ✅ 符合 MVP |
| 批次 2 生产级沙箱 | 41 §3 | 沙箱 verdict 字段已建；执行器在 evorule-server 侧 | ⚠️ 部分 |
| 批次 3 多副本 / 多白标 / 可观测 | 41 §4 | — | ❌ 0%（按"按需"原则） |

**MVP 完成度约 85%**；**生产级完成度约 5%**（与 41 规划对齐：生产级 = 批次 1+2+3 = 全部待做）。

---

## 2. 已发现的走偏点（按重要度排序）

### 🔴 重大走偏（影响生产部署）

| # | 走偏 | 设计要求 | 实际实现 | 注释 |
|---|---|---|---|---|
| **1** | **数据库** | 41 §2 批次 1 = PostgreSQL（多租户并发、行级 tenant_id、可扩展） | **SQLite（rusqlite bundled）** | 库结构、SQL 写法都不一样，从 SQLite 切到 PG 不是 schema migration 而是架构重写；41 §7 现状核实明确说"批次 1 PostgreSQL = 真缺口" |
| **2** | **密码哈希** | 43 §4 定案 = `Argon2id(m=19MiB,t=2,p=1)` | **手写 PBKDF2-HMAC-SHA256(600k 迭代)**，**且没用 `pbkdf2` crate**——`pbkdf2_hmac_sha256` 和 `hmac_sha256` 函数在 `src/auth/mod.rs:322,349` 纯手写 | `auth/mod.rs` 注释 L5-7 自己承认"离线环境无 `argon2` crate 采用 PBKDF2"。**手写密码学原语有安全风险**，但 MVP 阶段有意识取舍；生产级必须换 |
| **3** | **白标** | 39 决策点⑨ = `brand.yaml` 集中配置 + instance_id 进溯源 | **完全没实现**：仓内搜不到 `brand` / `whitelabel` 任何代码；`brand.yaml` 不存在 | AppState 里有 `instance_id` 字段（满足 39 §2"进溯源"），但**配置加载机制没有**——产品名、数据库名、logo、文案都是 Cargo.toml 硬编码 |
| **4** | **两层租户** | 38 §10 决策点⑧ = 平台层（hub/白标运营方）+ 组织层（自部署实例） | **只做了组织层单租户实例**，每实例一个 Tenant；`auth/mod.rs:3-9` 注释明确说"MVP 定案：单租户实例" | 符合 43 §11 MVP 边界，但与 38 §10 决策点走偏 |

### 🟡 小走偏（接口/算法不同但能工作）

| # | 走偏 | 设计要求 | 实际实现 | 影响 |
|---|---|---|---|---|
| **5** | **`content_hash` 算法** | 31 §4 "内容哈希去重"——evorule 全栈 BLAKE3 | `entry.rs:56-63` 用 `std::hash::DefaultHasher`（**非加密哈希**） | 内容哈希去重还能用，**但与 evorule 引擎侧 BLAKE3 chain 不一致**——未来做跨系统审计/校验可能错位 |
| **6** | **快照包内容哈希** | 36 §3 "全包内容哈希防篡改" | `bundle/mod.rs:16` 用 **SHA-256**（sha2 crate） | 同上，与 evorule 引擎 BLAKE3 不一致 |
| **7** | **生命周期枚举序列化** | 30 §B ④ "Draft→Candidate→Active→Published→Rejected" | `lifecycle.rs:14` PascalCase：`"Draft"` | evorule-server 实际 workspace state 用 **snake_case** `"draft"`；**跨系统数据交换会出 mismatch** |
| **8** | **错误响应结构** | 44 §5 = `{ "error": { "code", "message", "details" } }` | `api/mod.rs:117-125` 只有 `code + message`，**无 `details` / `trace_id`** | 不影响功能；排查问题时少 trace_id 定位 |
| **9** | **`headers_templates` 字段名** | 35 §5 = `headers_template`（单数） | `dependency.rs:42,116` = `headers_templates` / `headers_template`（混用） | 同模块内命名不一致；**轻微洁癖**问题 |
| **10** | **`PBKDF2` 实现未走 crate** | — | Cargo.toml `sha2 + rand + base64 + subtle` 全有，**没有 `hmac` crate**——`auth/mod.rs:322,349` 手写 HMAC-SHA256 + PBKDF2-HMAC-SHA256 | 没有经过独立审计；自写密码学实现是 OWASP 允许但要非常小心 |

### 🟢 已知后置（设计文档本身标注，符合 MVP 边界）

| # | 缺失 | 设计依据 |
|---|---|---|
| 11 | 跨系统身份映射 `user_xref` 表 | 41 §5.3"身份一致性"标注后置 |
| 12 | `dataset_versions` 快照表 | `resolve/mod.rs:9` 注释"由 45 号提供"——45 号本身就是生产级批次 1 规划 |
| 13 | OIDC SSO | 43 §4 MVP 边界 = 密码登录；SSO 后置 |
| 14 | RS256 JWT | 43 §7 MVP 边界 = HS256；`auth/mod.rs:71` 注释明确"生产级 45 号换 RS256" |
| 15 | rate limit / 配额 | 41 §2 批次 1 后置 |
| 16 | 异步 LLM 任务队列 | 37 §4 MVP 同步；task_id 字段已预留 |
| 17 | 多副本 / K8s / 可观测 | 41 §4 批次 3 标注"按需（2-3 个白标户/商业化时）" |
| 18 | 多白标运营 | 39 §3/§6 与 41 §4 批次 3 同上 |

---

## 3. 做得好的部分（与设计 100% 对齐的细节）

这些点很扎实，列出来防止被走偏点遮蔽：

1. **不可变性约束实现彻底**：`RuleEntry.is_frozen()` + `StoreError::EntryFrozen` 错误码 + `IllegalTransition` 错误码 + `EntryNotDeletable` 错误码——`store/mod.rs:151-165` 把"Active/Published 不可改、非法迁移、不可删除"全部变成显式 `ApiError::conflict`，**不静默降级**。
2. **版本链只增不改的硬约束**：`version.rs:187-191` `bump()` 检查 `next <= current || self.chain.contains(next_str)`，且 `validate()` 校验 `current == chain.tail()`。版本号不可复用 + 链单调。
3. **发布需独立审批**：`StoreError::PublishRequiresApproval` —— 即使状态是 Active，发布到 Published 必须走独立审批（30 §B ④ 强约束）。`/datasets/{id}/publish` 与 `/datasets/{id}/lifecycle` 是两个独立端点（api/mod.rs:269-271）。
4. **LLM 边界强约束落地**：`validate/mod.rs:25-27` `LlmGeneratedNotDraft` 校验——`llm_generated.flag=true` 的条目 status 只能是 Draft，否则显式报错。`Governance::is_llm_generated()` 提供业务侧调用。
5. **符号三方一致**：`validate/mod.rs:37-60` `io_services_from_rule_body` 提取 transform 里所有 `io_request.service_name`；`BundleError::ServiceNotDeclared` / `ServiceNotInRuleBody` 双向校验。
6. **回写通道只定 schema 不实现**——`bundle/mod.rs:8` 注释明确"回写通道 MVP 不实现：只定 schema（36 号 §6），不实现采集与闭环"，**严守 MVP 边界**。
7. **审计 only-append**：`entry_state_history` 表 + `Lifecycle.state_history: Vec<StateChange>` 只增不改，与 05 审计即记忆一致。
8. **不可白标掩盖（AGPL 底线）**：`lib.rs:11` 注释 "AGPL-3.0-or-later（双授权模式 = AGPL + 商业，合规底线，不可白标掩盖）"——产品哲学落地。
9. **双认证通道**：`api/mod.rs:178-198` `require_auth` 中间件 + `api_key_from_header` (L208-214) 双支持 Bearer / X-Api-Key，且 `bundle` 端点故意**不挂**通用 Bearer 中间件（`mod.rs:230-236` 注释 L231-232）——执行侧 X-Api-Key 拉取不被 401 拦截。
10. **租户内 SQL 隔离**：`ensure_default_tenant` 强制每实例一个 tenant；store 几乎所有查询都带 `tenant_id` 过滤（与 38 §10 SQL 层 tenant_id 隔离一致）。
11. **零转译原则**：`entry.rs:39` 注释 "evorule 原生 JSON，零转译（锚定 10_role13_demo.json）" + `validate/mod.rs:36-60` 直接解析 transform 树（不抽到 IR）——符合 30 §B ① 决策点。
12. **resolve 算法实现完整**：`resolve/mod.rs:77-` `resolve_auto` + `resolve_pinned` + `EffectiveRange` + 不静默降级（"日期无生效版本 → 显式报错"，L6 注释）。
13. **测试覆盖**：每个 model 模块都有 `#[cfg(test)] mod tests`，关键场景（serde roundtrip、is_frozen、content_hash_stable、bump_major_and_patch、bump_reuse_rejected、version_ordering、validate_catches_corruption）都覆盖。
14. **API 端点设计**：`api/mod.rs:225-348` 路由表清晰，public / protected / bundle 三层 Router 分离；`State<AppState>` 注入；端点路径全部 `/v1` 前缀；`PATCH /v1/datasets/{id}/lifecycle` 状态机迁移（44 §6 端点契约）。

---

## 4. 待用户决策 / 建议后续动作

### A. 走偏是否要回补

| 走偏 | 建议 |
|---|---|
| **SQLite → PostgreSQL**（最大走偏） | **是**：41 §7 现状核实明确指出"PostgreSQL = 真缺口，需新增 Postgres 支持"。**SQLite → PG 是架构级重写**（不是 schema migration），需要用户拍板是否启动批次 1 |
| **PBKDF2 → Argon2id** | **是**：加 `argon2` crate 改写 `auth/mod.rs:101-105` 一个函数，**成本低**。当前离线环境如果没 `argon2` crate，可以从 crates.io USTC sparse 镜像拉（你机器有 sparse 镜像） |
| **白标 brand.yaml** | **本期不必**：39 §3/§6 与 41 §4 批次 3 同步——"按需"原则，2-3 个白标户/商业化时再做 |
| **两层租户** | **本期不必**：与白标绑一起；38 §10 决策 + 41 §4 同 |
| **content_hash → BLAKE3** | **建议**：与 evorule 引擎对齐，避免跨系统审计时 hash 错位；`blake3` crate 是 Rust 生态标准，单文件 1KB |
| **生命周期 PascalCase → snake_case** | **必须回改**：跨系统数据交换会爆；**只改 `#[serde(rename_all = "...")]` 一个 attr** |
| **错误结构补 details/trace_id** | **建议**：与 41 §3 批次 2"异步 LLM 任务队列 + 重试不丢状态"配套；缺 trace_id 出问题难定位 |
| **`headers_templates` 命名统一** | **洁癖**：成本极低，建议改 |

### B. 没 git tag

Cargo.toml `version = "0.1.0"`，**但 `git tag` 列表空**。8 个 commit + ~340KB 源码 + 全 100% MVP 决策点闭合 → **建议打 v0.1.0 tag**（不是 v0.2.0，evorule-rule 仓独立版本号从 0.1.0 起）。

### C. Cargo build 状态未知

我**没跑 `cargo check` / `cargo test`**。如果你要我跑，告诉我—— evorule-rule 是独立仓，与 evorule 0.3.1 工作树未提交改动**无依赖关系**（Cargo.toml 无 `path = "../..."`），可以独立 build。

---

## 5. 一句话总结

> **evorule-rule 仓按 30-39 + 41-A/B 设计基线，完成了 8/9 MVP 决策点（白标 39 缺失），数据模型 + 版本语义 + 生命周期 + 快照包 + LLM 接口 + 鉴权 + API 全部落地，工作量扎实**。**但有 4 处重大走偏**（SQLite vs PostgreSQL、手写 PBKDF2 vs Argon2id、无白标、单租户）**和 5 处小走偏**（hash 算法不统一、枚举大小写不统一、错误体缺 details）；**按 MVP 边界看是 85% 完成度，按生产级 41 规划看是 5%**。**建议下一轮先跑 `cargo check` 验证可编译，然后按上面 6 项回补优先级排期**。
